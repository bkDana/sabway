<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="bucket">
 	<insert id="insertBucket" parameterType="bucket">
 		insert into
 			bucket
 		values
 			(
 			seq_buc_idx.nextval,
 			#{bucCustomerIdx},
 			#{bucCusoIdx},
 			#{bucBread},
 			#{bucMain},
 			#{bucVegi},
 			#{bucCheese},
 			#{bucSource},
 			#{bucTopping},
 			#{bucSide},
 			#{bucIsSalad},
 			#{bucIsOvened},
 			#{bucSet},
 			#{bucCost},
 			#{bucDiscntRate},
 			#{bucKcal},
 			#{bucQuantity},
 			default
 			)	
 	</insert>
	<insert id="tempOrderInsert" parameterType="bucket">
 		insert into
 			bucket
 		values
 			(
 			seq_buc_idx.nextval,
 			#{bucCusoIdx},
 			#{bucBread},
 			#{bucMain},
 			#{bucVegi},
 			#{bucCheese},
 			#{bucSource},
 			#{bucTopping},
 			#{bucSide},
 			#{bucIsSalad},
 			#{bucIsOvened},
 			#{bucSet},
 			#{bucCost},
 			#{bucDiscntRate},
 			#{bucKcal},
 			#{bucQuantity},
 			default,
 			#{bucCustomerIdx}
 			)	
 	</insert>
 	<select id="selectAllBucket" parameterType="_int" resultType="bucket">
 		select
 			buc_bread as bucBread,
 			buc_main as bucMain,
 			buc_vegi as bucVegi,
 			buc_cheese as bucCheese,
 			buc_source as bucSource,
 			buc_topping as bucTopping,
 			buc_side as bucSide,
 			buc_is_salad as bucIsSalad,
 			buc_is_ovened as bucIsOvened,
 			buc_set as bucSet,
 			buc_cost as bucCost,
 			buc_discnt_rate as bucDiscntRate,
 			buc_kcal as bucKcal,
 			buc_quantity as bucQuantity,
 			buc_reg_date as bucRegDate,
 			buc_customer_idx asbucCustomerIdx,
 			buc_is15 as bucIs15
 		from bucket
 		where
 			buc_customer_idx = #{bucCustomerIdx}
    </select>
 	<select id="ingreSelectAll" resultType="ingre">
 		select ingre_idx as ingreIdx, ingre_label as ingreLabel, ingre_type as ingreType, ingre_cost15 as ingreCost15, ingre_cost30 as ingreCost30, 
 		ingre_discnt_rate as ingreDiscntRate, ingre_kcal as ingreKcal, ingre_active as ingreActive,
 		ingre_description as ingreDescription, ingre_filepath as ingreFilepath,ingre_reg_date as ingreRegDate
 		from ingredients
 	</select>
</mapper>
